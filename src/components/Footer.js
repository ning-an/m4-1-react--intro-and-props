import React from "react";

import "./Footer.css";

const Footer = () => {
  return (
    <footer>
      <input type="text" className="textInput"></input>
    </footer>
  );
};

export default Footer;
